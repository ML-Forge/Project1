# to call a function inside a class \

Configuration().get_data_validation_config() # get_data_validation_config is the function inside the class called "Configuration."

#whenever an object of a class is called - it calls the __init__ function of that class also. 